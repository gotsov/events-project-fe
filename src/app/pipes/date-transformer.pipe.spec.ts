import { DateTransformerPipe } from './date-transformer.pipe';

describe('DateTransformerPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTransformerPipe();
    expect(pipe).toBeTruthy();
  });
});
